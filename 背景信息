C港法国乘客的存活率高，可能与他们头等舱乘客多有关



项目分析思路
1. 明确业务目标与需求
2. 数据收集与理解
3. 探索性数据分析

数据分析各种图以及作用

把每一类数据和目标数据都放在一起单独分析


如何处理缺失值
1. 删除缺失值：当缺失值比例极小，或者缺失数据所在的特征/样本对整体分析影响不大时，最直接的方法就是删除。
2. 统计量填充：
    均值填充 (Mean)： 使用该列非缺失数据的平均值进行填充。适用于数据分布相对均匀、没有极端异常值的情况。
    中位数填充 (Median)： 使用该列的中位数进行填充。当数据存在明显偏态或包含极端异常值时，中位数比均值更具鲁棒性。
    众数填充 (Mode)： 通常用于分类数据（如性别、城市）或离散型数值数据，使用该列出现频率最高的值进行填充。
3. 时间序列/序列数据填充：
    在处理连续的时间序列数据（例如在回测框架中处理日线或分钟线的行情数据、计算移动平均线等技术指标前的数据对齐）时，不能简单使用全局均值，以防破坏时间序列的连续性或引入逻辑错误。
    前向填充 ： 用缺失值前面的最后一个有效数据进行填充。在金融数据分析中非常关键，因为它可以有效防止引入“未来数据”（Look-ahead bias）。
    后向填充 ： 用缺失值后面的第一个有效数据填充（需谨慎使用）。
    线性插值： 假设相邻两个有效数据点之间是线性变化的，通过计算两点之间的直线上的值来估算缺失值。适用于趋势平稳变化的数据。
4. 算法预测插补：
    将缺失的特征作为目标变量，利用其他完整的特征训练机器学习模型来预测并填补缺失值。
    K近邻 (KNN) 插补： 寻找与缺失样本在其他特征上最相似的K个样本，利用它们的加权平均值或众数来填补。
    随机森林 (MissForest) / 回归预测： 构建回归或分类模型来预测缺失值，精度较高，但计算成本也相对较大。

merge用法
合并两个表

pd.merge(表A, 表B, on="共同列名")

on="列名"           # 按某列合并（最常用）
left_index=True,    # 左表按索引对齐
right_index=True    # 右表按索引对齐

how="inner"   # 只保留两边都有的（默认）
how="left"    # 以左表为准，右表没匹配到的填 NaN
how="right"   # 以右表为准
how="outer"   # 全保留，没匹配到的填 NaN

suffixes=["_左表后缀", "_右表后缀"]   # 当两个表有相同列名时，用后缀区分

groupby用法
groupby能返回DataFrame和Series两种数据类型

df.groupby("列名")           # 按某列分组
df.groupby(["列1", "列2"])   # 按多列分组
df.groupby(level=0)          # 按索引层级分组（多级索引时用）

.groupby("Embarked").sum()        # 求和
.groupby("Embarked").mean()       # 平均值
.groupby("Embarked").count()      # 计数（算非空值）
.groupby("Embarked").max()        # 最大值
.groupby("Embarked")["Survived"].value_counts()  # 对指定列Embarked分类后，再拿Survived继续分类，.value_counts() 是值出现了几次



